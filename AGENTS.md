# 📄 AGENTS.md — 协同开发协议与规范

本文件规定了 AI 编码助手（Antigravity）与开发者进行 miniOB 数据库内核开发时的**核心工作流、日志规范、文件读写约束、代码风格以及回归测试流程**。

---

## 1. 核心工作流与步骤规范

每一轮功能开发必须严格遵循以下阶段要求：

### 1.1 开发前阶段（Pre-Development Check）

在动手修改任何代码前，AI 助手必须：

1. **测试用例导向**：首先查阅该功能的官方 `.test` 文件与自定义 `.sql` 测试用例（如 `primary-null.test`），**梳理出该功能的所有边界场景**（如空值比较、空表表现、异常输入等）。
2. **影响链溯源**：在修改任何模块（例如存储层 Page 结构）前，必须**向上溯源**它在 `SQL 解析器`、`语义检查层(Resolver)` 及 `执行器算子` 中的引用链，列出所有受影响的文件列表，防止牵一发而动全身。
3. **只读探索**：在修改文件前，AI 必须完整读取目标文件上下关联的代码（建议至少阅读前后 50 行），充分理解其设计意图。

### 1.2 开发中阶段（In-Development Rules）

1. **高内聚低耦合**：尽量在现有框架下进行局部扩展，避免无意义的重构或大规模改写。
2. **保护既有逻辑**：非必要不删除已实现功能的已有代码、注释和 Docstrings。
3. **修改范围最小化**：使用精确的局部代码块替换，禁止全文件重写以节省上下文并降低错误率。

### 1.3 开发后与回归测试阶段（Post-Development Verification）

完成代码修改后，必须执行以下步骤进行闭环验证：

1. **代码格式化**：检查并确保新增代码符合项目的 C++ 风格（遵循 `.clang-format` 规范）。
2. **编译验证免除（当前协定）**：
   > [!NOTE]
   > 当前阶段 AI 助手的职责调整为：**优先完成该部分所需功能的完整代码实现，不要求在本地执行编译验证**。  
   > `cmake --build build -j4` 的编译检查、编译错误修复、以及后续联调工作，统一交由开发者同伴负责。  
   > 因此，AI 助手在本阶段可以不主动执行 Docker 编译；如确有必要，可在日志中注明“尚未本地编译，待同伴接手验证”。
3. **测试验证免除（最新协定）**：
   > [!NOTE]
   > 经最新协商，**每一部分的测试执行及验证工作临时交由同伴异步完成，AI 助手当前只负责“该部分所需功能的完整代码编写”**。因此，AI 助手无需在本地运行全量/快速测试回归脚本（例如 `run_all.py`）；测试结果由同伴后续接手确认。
4. 每一阶段开发进行中与完成后，必须严格执行开发日志记录。由于编译、测试与后续调试均交由同伴执行，开发日志中的结果状态可简记为“功能代码已完成，待同伴编译/调试/测试对接”。

---

## 2. 个人工作区开发日志规范（XC 工作区）

每一阶段开发进行中与完成后，必须严格执行开发日志记录：

### 2.1 日志路径

* 所有开发日志必须写入到工作区日志目录下：`lab/log/xc/`。
* 如果 `xc/` 子目录不存在，AI 助手将在第一次写入时自动创建该目录。

### 2.2 日志记录与更新原则（单日志锁原则）

1. **同一阶段日志唯一性**：在某一开发阶段（如 `null` 支持）完全通过测试并交付之前，**只允许更新和完善当前阶段的开发日志文件，禁止新建该阶段的其他日志文件**。
2. **过程记录原则（做到哪里写到哪里）**：
   * 开发日志应随着开发推进持续更新，遵循“**做到哪里写到哪里**”。
   * 已完成的分析、设计判断、已确认的边界、已发现的阻塞、已落地的代码点，都应及时补充到当前阶段唯一日志中。
   * 不要求每一步都立即编译或测试后再写日志；日志首先服务于真实开发进展的同步与留痕。
3. **失败时即时退出汇报**：
   * 开发过程中如果遇到任何失败情况（如编译失败、单元测试未通过、回归率下降等）：
     * **AI 助手必须立即停止当前开发进程，即时退出**。
     * **将当前遇到的失败详情（报错日志、未通过用例、当前进度等）完整写入当前的开发日志文件中**。
     * **立刻向开发者进行详细汇报**，请求开发者协助对齐或修改设计方案。
4. **成功交付**：只有当当前阶段的所有测试完全通过、日志完美记录后，方可启动下一个阶段的日志文件。

---

## 3. 文件读写与修改规范

1. **精准读写**：
   * AI 在读取代码时，优先使用行数限制的 `view_file`，精准获取目标函数逻辑。
   * 修改代码时，必须精细定位 `StartLine` 和 `EndLine`，只对发生变动的 contiguous block 展开修改。
2. **元数据与文件变动同步**：
   * 如果修改了存储引擎的物理格式（如表元数据 `table_meta` 或页面头 `PageHeader`），必须同步更新其**序列化与反序列化逻辑**，并设计好兼容性或清空旧数据库文件的预案。

---

## 4. C++ 代码开发风格

为保持 miniOB 项目的简洁与专业性，所有新增的 C++ 代码需遵循以下风格：

1. **命名规范**：
   * 变量、函数与参数采用**小写蛇形命名法**（`snake_case`），例如 `is_null`，`record_capacity`。
   * 类名与结构体采用**驼峰命名法**（`CamelCase`），例如 `RowRecordPageHandler`。
   * 宏定义与常量采用**大写蛇形**（`UPPER_SNAKE_CASE`），例如 `BP_INVALID_PAGE_NUM`。
2. **防御性编程与错误处理**：
   * 充分使用项目内置的 `RC` (Return Code) 错误传递机制。**绝不允许吞掉错误**。所有可能失败的操作必须进行 `OB_FAIL` 或直接返回 `rc`。
   * 示例：

     ```cpp
     rc = get_record(rid, record);
     if (rc != RC::SUCCESS) {
       LOG_WARN("failed to get record, rc=%s", strrc(rc));
       return rc;
     }
     ```

3. **断言与日志**：
   * 使用 `ASSERT` 约束核心的不变量（如指针非空、数组不越界）。
   * 在关键控制流（如建表、DDL、事务起止、异常分支）中，使用 `LOG_INFO` / `LOG_WARN` / `LOG_ERROR` 输出带上下文的日志，方便 Debug。
4. **现代 C++ 规范**：
   * 尽量使用 `std::unique_ptr` 和 `std::move` 进行内存生命周期管理，避免裸指针导致的内存泄漏。

---

## 5. 协作沟通规范

1. **方案先行**：在进行 **`null` 支持**、**子查询**等大型、多模块联动的任务时，AI 必须先提供一份详细的**实现方案（Implementation Plan）**，由开发者核验设计思路、数据排布结构后，再动手写代码。
2. **保持谦逊与严谨**：汇报开发成果时，AI 应以事实为依据（例如哪些测试通过了，耗时多少），避免使用过度自信的语气（如“完美解决”、“100%无错”）。

---

## 6. 课程设计报告写作规范

本小节用于约束 AI 在生成、修改、润色或补全 **课程设计报告**（`lab/doc/` 下 Markdown / Word 文稿）时的写作风格、内容组织、技术表达和格式要求。所有课程设计报告相关内容均应优先遵循本节。

### 6.1 写作定位

课程设计报告不是技术博客、项目复盘、产品介绍或教材知识总结，而是面向课程考核的工程实践类文档。

报告应重点体现：设计目标是否明确；需求分析是否完整；系统方案是否合理；模块划分是否清晰；实现过程是否具体；测试验证是否充分；结果分析是否有依据；总结与改进是否客观。

报告的核心是 **「设计过程说明」** 和 **「工程实现分析」**，而不是单纯介绍相关概念。

### 6.2 总体文风

报告应采用正式、客观、规范的书面表达，整体风格接近软件工程课程设计报告、本科课程设计报告、工程技术文档、系统设计说明书。

避免写成：技术博客、产品宣传文案、项目介绍文案、学习笔记、教材知识复述、个人心得体会。

### 6.3 叙述方式

**叙述主体**：优先使用「本设计」「本课题」「本系统」「该模块」「测试结果」等客观表述；避免频繁使用「我」「我们」「笔者」「小组成员」。

**语气要求**：以客观陈述为主，不使用夸张、情绪化或宣传式表达。

**结论表达**：涉及性能、效果、正确性、稳定性等结论时，应尽量基于实现过程、测试结果或实验数据说明；禁止无依据的「效果很好」「非常稳定」等断言。

### 6.4 内容结构（默认）

生成完整课程设计报告时，默认采用以下结构（若学校模板或 `lab/题目/指导书.md` 另有固定目录，应优先遵循已有目录）：

1. 引言  
2. 课题背景与设计目标  
3. 需求分析  
4. 总体方案设计  
5. 系统详细设计  
6. 系统实现  
7. 系统测试与结果分析  
8. 存在问题与改进方向  
9. 总结  
10. 参考文献  

### 6.5 各章节要点

- **引言**：说明课题来源、设计内容、报告组织；不宜大篇幅堆砌概念定义。  
- **需求分析**：从功能需求与非功能需求展开；不应只列功能名称。  
- **总体方案设计**：说明整体架构、模块划分、数据流向、技术选型及原因。  
- **系统详细设计**：围绕模块、接口、关键流程展开；可配合架构图、流程图。  
- **系统实现**：说明「如何实现」——输入、校验、处理、存储/输出、异常处理；代码仅作辅助，不占主体。  
- **测试与结果分析**：须含测试环境、方法、用例、结果、分析；测试用例宜表格呈现；禁止只写「测试通过」。  
- **总结**：客观归纳完成情况与不足；避免写成个人感想。

### 6.6 理论介绍篇幅

理论基础不应成为主体。默认建议：背景约 5%、理论约 10%、需求约 15%、设计约 25%、实现约 30%、测试约 10%、总结约 5%。禁止大篇幅解释与具体设计决策无关的通用概念。

### 6.7 技术表达

优先使用：「系统采用……」「模块负责……」「测试结果表明……」「为保证……，模块设计了……」等工程化表述。

涉及设计决策时应说明：为何这样设计、解决什么问题、当前方案的限制。

功能实现应按流程展开：输入来源 → 数据校验 → 处理逻辑 → 存储或输出 → 异常处理 → 结果反馈。

### 6.8 图表与格式

涉及结构、流程、测试结果时应优先使用表格、Mermaid 流程图等；图表前后须有文字说明。默认 Markdown 输出；标题层级清晰；正文不得加入与报告无关的闲聊或 AI 提示语。

### 6.9 禁止事项

- 大量第一人称、口语化、宣传式表达  
- 编造实验数据、测试结果、参考文献  
- 大篇幅复述教材概念  
- 只描述现象不解释实现过程  
- 只给结论不给依据  
- 将报告写成 README 或个人心得  

### 6.10 修改已有报告时

优先保持原有目录结构和技术事实不变；提升正式程度、补充设计原因、强化实现与测试分析；**不得擅自改变**系统功能、技术栈、已有测试结果或参考文献。

### 6.11 本课题特殊约定

- 题目清单以 `lab/题目/指导书.md` 第三章 **25 道题目** 为准；正文仅标注难度，验收分值见指导书末尾「实验验收表格」。  
- 报告**不要求**对 25 题全部完成代码实现；当前已实现功能范围以仓库代码与 `lab/test/latest.md` 为准，报告中对未完成题目仅作简要列示，不宜展开冗长辩解。  
在 AGENTS.md §6 课程设计报告写作规范中，叙事密度与章节比例可参考同类型课程报告（「相关技术 → 功能需求 → 系统设计与实现（主体）→ 测试 → 总结」）；§3 宜占全文约六成，以段落叙述为主，表格与代码片段为辅。
